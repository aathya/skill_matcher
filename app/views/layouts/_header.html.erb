<% if user_signed_in? %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
    <%= link_to 'Skill Matcher', root_path , class: 'navbar-brand mr-auto' %>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a aria-expanded="false" aria-haspopup="true" class="nav-cursor nav-link dropdown-toggle" data-toggle="dropdown" id="navbarDropdownMenuLink">
          <%= "Hi #{current_user.name}" %>
        </a>
        <div aria-labelledby="navbarDropdownMenuLink" class="dropdown-menu dropdown-menu-right">
          <%= link_to 'My Profile', edit_user_registration_path, class: 'nav-link' %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'nav-link' %>
        </div>
      </li>
    </ul>
  </nav>
<% end %>
